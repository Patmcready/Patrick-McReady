/* style.css */
body {
    font-family: sans-serif;
    margin: 0; /* Remove default margins */
    line-height: 1.6;
    color: #333; /* Dark gray text */
}

header {
    background-color: #f0f0f0; /* Light gray background for header */
    padding: 20px;
    text-align: center;
}

nav ul {
    list-style: none;
    padding: 0;
    margin: 0;
}

nav li {
    display: inline;
    margin: 0 15px;
}

nav a {
    text-decoration: none;
    color: #007bff; /* Blue link color */
}

section {
    padding: 40px;
}

h2 {
    margin-bottom: 20px;
}

.profile-image {
    float: left; /* Image on the left */
    margin-right: 20px;
    width: 150px;
    border-radius: 50%; /* Make it round */
}

.project {
    margin-bottom: 30px;
    border: 1px solid #ddd; /* Light gray border */
    padding: 20px;
}

.project img {
    max-width: 100%; /* Make sure image fits container */
    height: auto;
    display: block; /* Prevent image from affecting text flow */
    margin-bottom: 10px;
}

footer {
    background-color: #333;
    color: #fff;
    text-align: center;
    padding: 10px;
    position: relative; /* Footer at bottom */
    bottom: 0; /* Footer at bottom */
    width: 100%; /* Footer full width */
}

/* Basic Responsive Design */
@media (max-width: 768px) {
    .profile-image {
        float: none; /* Image above text on smaller screens */
        margin: 0 auto 20px; /* Centered */
        display: block; /* Prevents image from affecting text flow */
    }
    nav li {
        display: block; /* Vertical navigation on smaller screens */
        margin: 10px 0; /* Space between vertical nav items */
    }
}