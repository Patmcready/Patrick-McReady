/* style.css */

body {
    font-family: 'Roboto', sans-serif; /* Modern, tech-focused font */
    margin: 0;
    line-height: 1.6;
    color: #333; /* Dark gray text for readability */
    background-color: #f4f4f4; /* Light gray background */
}

/* Header Styles */
header {
    background-color: #007bff; /* Blue header background */
    color: white;
    padding: 20px;
    text-align: center;
}

h1 {
    font-size: 2.5em;
    margin-bottom: 10px;
}

nav ul {
    list-style: none;
    padding: 0;
    margin: 0;
}

nav li {
    display: inline;
    margin: 0 15px;
}

nav a {
    text-decoration: none;
    color: white;
    font-weight: bold;
    transition: color 0.3s ease; /* Smooth color transition on hover */
}

nav a:hover {
    color: #f0f0f0; /* Lighter color on hover */
}

/* Section Styles */
section {
    padding: 40px;
    background-color: white; /* White background for content sections */
    margin-bottom: 20px; /* Spacing between sections */
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
    border-radius: 8px; /* Rounded corners */
}

h2 {
    color: #007bff; /* Blue headings */
    margin-bottom: 20px;
    border-bottom: 2px solid #ddd; /* Light gray line under headings */
    padding-bottom: 10px;
}

/* Profile Image */.profile-image {
    float: left;
    margin-right: 20px;
    width: 150px; /* Smaller profile picture */
    height: 150px; /* Maintain aspect ratio if needed */
    border-radius: 50%; /* Make it round */
    border: 3px solid #007bff; /* Blue border */
    object-fit: cover; /* Prevents image distortion */
}

/* Experience Styles (Resume Page) */.experience {
    margin-bottom: 30px;
    border: 1px solid #ddd;
    padding: 20px;
    border-radius: 5px; /* Slightly rounded corners for experience blocks */
}.experience h3 {
    margin-top: 0;
    color: #333; /* Darker heading for experience titles */
}.experience p {
  margin-bottom: 10px; /* Space between paragraphs in experience */
}.experience ul {
    list-style-type: disc;
    margin-left: 20px;
    padding: 0;
}

/* Footer Styles */
footer {
    background-color: #333;
    color: white;
    text-align: center;
    padding: 10px;
    position: relative;
    bottom: 0;
    width: 100%;
}

/* Responsive Design (Basic) */
@media (max-width: 768px) {
  .profile-image {
        float: none;
        margin: 0 auto 20px;
        display: block;
        width: 120px; /* Even smaller on mobile */
        height: 120px;
    }

    nav li {
      display: block; /* Stack navigation vertically */
      margin: 10px 0;
    }

    nav a {
      display: block; /* Make links full width on small screens */
      padding: 10px; /* Add some padding around links */
      text-align: center; /* Center the text */
    }
}


/* Add any other styles you need here */